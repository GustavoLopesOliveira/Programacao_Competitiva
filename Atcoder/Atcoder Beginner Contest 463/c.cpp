#include <bits/stdc++.h>
#define ll long long
#define pll pair<ll,ll>

using namespace std;


//Unsolved
void solve(){
    ll n; cin >> n;

    unordered_map<ll,priority_queue<ll,vector<ll>,greater>> hashmap;
    for(ll i = 0; i < n; i++){
        ll h,l; cin >> h >> l;

    }

    ll q; cin >> q;
    vector<ll> t(q);
    for(ll i = 0; i < q; i++) cin >> t[i];
}

int main(){
    solve();
}

